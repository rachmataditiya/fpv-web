# RFC 0000: Title

- **Status:** draft | accepted | implemented | rejected
- **Issue:** #NNN
- **Author:** @handle

## Problem

What player/contributor problem does this solve? Why now?

## Proposal

The design, concretely. For gameplay features include the *feel* target
(rates, timings, ranges). For systems include which existing contracts it
touches (input pipeline, `CollisionWorld`, `TrackDef`, sim determinism).

## Alternatives considered

Short list with the reason each lost.

## Verification plan

How we'll know it works: unit tests, `__fpv.step()` scripted checks, and the
manual flight matrix (which inputs × which maps).
